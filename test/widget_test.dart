import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — proper widget tests require mock providers
    expect(1 + 1, equals(2));
  });
}
